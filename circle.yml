machine:
  services:
    - docker

test:
  pre:
    - docker-compose -f docker-compose.test.yml up -d
  override:
    - docker-compose exec web python manage.py test
  post:
    - docker-compose down
