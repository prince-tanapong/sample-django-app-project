version: '2'
services:
  db:
    image: postgres
  web:
    build: .
    command: bash -c "sleep 5 && python manage.py migrate && python manage.py runserver 0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
